const path = require('path');

/**
 * Le dice a Sequelize-CLI dónde está cada cosa, ya que tu estructura no es la
 * que la CLI asume por defecto. Este archivo va en la RAÍZ del repo.
 *
 * Todos los comandos de la CLI (db:seed:all, db:migrate, etc.) deben ejecutarse
 * desde la raíz del repo para que estas rutas relativas resuelvan.
 */

module.exports = {
  config:           path.resolve('backend', 'config', 'database.config.js'),
  'models-path':    path.resolve('backend', 'models'),
  'seeders-path':   path.resolve('backend', 'seeders'),
  'migrations-path':path.resolve('backend', 'migrations'),
};
